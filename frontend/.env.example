# Backend API origin. Defaults to http://127.0.0.1:8787 for local dev
# when unset. Set this in Vercel (or your frontend host) to point at your
# deployed backend, e.g. https://your-backend.up.railway.app
NEXT_PUBLIC_API_BASE=http://127.0.0.1:8787
